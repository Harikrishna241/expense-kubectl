# A simple example of ClusterConfig object:
---
apiversion: eksctl.io/v1alpha5
kind: ClusterConfig 
metadata: 
  name: Expense
  region: us-east-1

managedNodeGroups:
- name: expense
  instancetype: m5.large
  desiredCapacity: 3
  spot: true
